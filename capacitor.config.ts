import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lenmot.construction',
  appName: 'LenmotConstruction',
  webDir: 'dist'
};

export default config;
